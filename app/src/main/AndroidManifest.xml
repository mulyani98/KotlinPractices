<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/kotlin_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/kotlin_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KotlinPractices"
        tools:targetApi="31" >

        <activity android:name=".HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".TaskOneActivity"
            android:exported="false">
        </activity>

        <activity android:name=".TaskTwoActivity"
            android:exported="false">
        </activity>

        <activity android:name=".KotlinNotesActivity"
            android:exported="false">
        </activity>

        <activity android:name=".GoogleSignInActivity"
            android:theme="@style/Theme.KotlinPractices"
            android:exported="false">
        </activity>

        <activity android:name=".OldGoogleSignInActivity"
            android:exported="false">
        </activity>

        <activity android:name=".AboutActivity"
            android:exported="false">
        </activity>

        <activity android:name=".WebViewActivity"
            android:exported="false">
        </activity>

        <meta-data
            android:name="com.google.android.gms.signin.CLIENT_ID"
            android:value="578060869145-n6671mce6ld8akc2hvt0obhtfmohu80i.apps.googleusercontent.com" >
        </meta-data>

    </application>

</manifest>